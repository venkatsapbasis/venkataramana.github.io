// Typed.js Effect
const typed = new Typed("#typed-text", {
  strings: ["Venkataramana Ramireddy", "SAP BASIS Consultant", "SAP ECS (Enterprise Cloud Services) project"],
  typeSpeed: 60,
  backSpeed: 30,
  loop: true
});

// AOS Initialization
AOS.init({
  duration: 1000,
  once: true
});
